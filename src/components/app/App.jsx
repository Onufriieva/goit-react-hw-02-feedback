import Section from "../section/Section";
import {ContainerApp} from './AppStyled'

export const App = () => {
  return (
    <ContainerApp>
      <Section total={0} percentage={0}/>     
    </ContainerApp>
  );
};
